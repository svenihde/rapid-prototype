== JEngine

The JEngine is a rapid prototype to get a better understanding of a modeling execution environment.


== INSTALL

* bundle install
* bin/rake db:migrate
* bin/rake db:seed 

:-)
